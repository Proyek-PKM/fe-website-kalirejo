const baseUrl = 'https://ephemeral.desakalirejo.id';

export const urlConfig = {
  getUser: `${baseUrl}/user/getuser`,
  reqLogin: `${baseUrl}/auth/request`,
  valLogin: `${baseUrl}/auth/request/verify`,
}

// export { urlConfig }
